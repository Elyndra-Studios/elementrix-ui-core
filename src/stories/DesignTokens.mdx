import { Meta } from '@storybook/blocks';

<Meta title="Design System/Tokens" />

<div style="font-family: var(--el-font-sans);">

# Design Tokens

All Elementrix UI components are built using a comprehensive design token system. These CSS variables ensure consistency and make theming effortless.

## Colors

### Neutral Palette

<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 1rem; margin: 2rem 0;">
  {[50, 100, 200, 300, 400, 500, 600, 700, 800, 900].map(shade => (
    <div key={shade} style={{textAlign: 'center'}}>
      <div style={{
        width: '100%',
        height: '80px',
        backgroundColor: `var(--el-neutral-${shade})`,
        borderRadius: '8px',
        border: '1px solid var(--el-border-color)',
        marginBottom: '0.5rem'
      }} />
      <div style={{fontSize: '12px', color: 'var(--el-neutral-600)'}}>
        neutral-{shade}
      </div>
    </div>
  ))}
</div>

### Primary / Brand

<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 1rem; margin: 2rem 0;">
  {[50, 100, 200, 300, 400, 500, 600, 700, 800, 900].map(shade => (
    <div key={shade} style={{textAlign: 'center'}}>
      <div style={{
        width: '100%',
        height: '80px',
        backgroundColor: `var(--el-primary-${shade})`,
        borderRadius: '8px',
        border: '1px solid var(--el-border-color)',
        marginBottom: '0.5rem'
      }} />
      <div style={{fontSize: '12px', color: 'var(--el-neutral-600)'}}>
        primary-{shade}
      </div>
    </div>
  ))}
</div>

### Semantic Colors

<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 1rem; margin: 2rem 0;">
  {[
    { name: 'success', color: 'var(--el-success-500)' },
    { name: 'warning', color: 'var(--el-warning-500)' },
    { name: 'error', color: 'var(--el-error-500)' },
    { name: 'info', color: 'var(--el-info-500)' }
  ].map(({ name, color }) => (
    <div key={name} style={{textAlign: 'center'}}>
      <div style={{
        width: '100%',
        height: '80px',
        backgroundColor: color,
        borderRadius: '8px',
        marginBottom: '0.5rem'
      }} />
      <div style={{fontSize: '12px', color: 'var(--el-neutral-600)'}}>
        {name}-500
      </div>
    </div>
  ))}
</div>

## Typography

### Font Families

```css
--el-font-sans: -apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', sans-serif;
--el-font-mono: 'SF Mono', 'Monaco', 'Cascadia Code', monospace;
```

### Font Sizes

<div style="margin: 2rem 0;">
  {[
    { name: 'xs', value: '0.75rem' },
    { name: 'sm', value: '0.875rem' },
    { name: 'base', value: '1rem' },
    { name: 'lg', value: '1.125rem' },
    { name: 'xl', value: '1.25rem' },
    { name: '2xl', value: '1.5rem' },
    { name: '3xl', value: '1.875rem' },
    { name: '4xl', value: '2.25rem' },
    { name: '5xl', value: '3rem' }
  ].map(({ name, value }) => (
    <div key={name} style={{
      padding: '1rem',
      borderBottom: '1px solid var(--el-border-color)',
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }}>
      <code style={{color: 'var(--el-primary-600)'}}>--el-text-{name}</code>
      <span style={{fontSize: `var(--el-text-${name})`}}>The quick brown fox</span>
      <span style={{color: 'var(--el-neutral-500)', fontSize: '14px'}}>{value}</span>
    </div>
  ))}
</div>

## Spacing

Based on an 8px grid system:

<div style="margin: 2rem 0;">
  {[
    { name: '0', value: '0' },
    { name: '1', value: '0.25rem' },
    { name: '2', value: '0.5rem' },
    { name: '3', value: '0.75rem' },
    { name: '4', value: '1rem' },
    { name: '5', value: '1.25rem' },
    { name: '6', value: '1.5rem' },
    { name: '8', value: '2rem' },
    { name: '10', value: '2.5rem' },
    { name: '12', value: '3rem' },
    { name: '16', value: '4rem' },
    { name: '20', value: '5rem' },
    { name: '24', value: '6rem' }
  ].map(({ name, value }) => (
    <div key={name} style={{
      padding: '0.75rem 1rem',
      borderBottom: '1px solid var(--el-border-color)',
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }}>
      <code style={{color: 'var(--el-primary-600)'}}>--el-space-{name}</code>
      <div style={{
        height: '24px',
        width: value,
        backgroundColor: 'var(--el-primary-200)',
        borderRadius: '4px'
      }} />
      <span style={{color: 'var(--el-neutral-500)', fontSize: '14px'}}>{value}</span>
    </div>
  ))}
</div>

## Border Radius

<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 1.5rem; margin: 2rem 0;">
  {[
    { name: 'none', value: '0' },
    { name: 'sm', value: '0.25rem' },
    { name: 'base', value: '0.375rem' },
    { name: 'md', value: '0.5rem' },
    { name: 'lg', value: '0.75rem' },
    { name: 'xl', value: '1rem' },
    { name: '2xl', value: '1.5rem' },
    { name: 'full', value: '9999px' }
  ].map(({ name, value }) => (
    <div key={name} style={{textAlign: 'center'}}>
      <div style={{
        width: '100%',
        height: '80px',
        backgroundColor: 'var(--el-primary-100)',
        borderRadius: `var(--el-radius-${name})`,
        marginBottom: '0.5rem',
        border: '2px solid var(--el-primary-300)'
      }} />
      <div style={{fontSize: '12px', color: 'var(--el-neutral-600)'}}>
        radius-{name}
      </div>
      <div style={{fontSize: '11px', color: 'var(--el-neutral-400)'}}>
        {value}
      </div>
    </div>
  ))}
</div>

## Shadows

<div style="margin: 2rem 0;">
  {[
    { name: 'xs', value: '0 1px 2px 0 rgb(0 0 0 / 0.05)' },
    { name: 'sm', value: '0 1px 3px 0 rgb(0 0 0 / 0.1)' },
    { name: 'md', value: '0 4px 6px -1px rgb(0 0 0 / 0.1)' },
    { name: 'lg', value: '0 10px 15px -3px rgb(0 0 0 / 0.1)' },
    { name: 'xl', value: '0 20px 25px -5px rgb(0 0 0 / 0.1)' },
    { name: '2xl', value: '0 25px 50px -12px rgb(0 0 0 / 0.25)' }
  ].map(({ name, value }) => (
    <div key={name} style={{
      padding: '2rem',
      marginBottom: '1.5rem',
      backgroundColor: 'var(--el-surface-base)',
      borderRadius: '8px',
      boxShadow: `var(--el-shadow-${name})`
    }}>
      <code style={{color: 'var(--el-primary-600)'}}>--el-shadow-{name}</code>
      <div style={{fontSize: '12px', color: 'var(--el-neutral-500)', marginTop: '0.5rem'}}>
        {value}
      </div>
    </div>
  ))}
</div>

</div>
